{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope @abb/common --scope @abb/server"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "name": "abb",
  "devDependencies": {
    "lerna": "^3.21.0"
  }
}
